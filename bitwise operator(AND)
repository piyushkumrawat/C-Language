#include<stdio.h>
int main()
{
    int a,b;
    /*Bitwise operator convert given value into bit(binary code) and multiply both codes*/
    scanf("%d\n",&a);
    scanf("%d",&b);
    printf("%d",a&b);
}
