#include <stdio.h>

int main()
{
    int a,b,c;
    printf("Enter value of a=");
    scanf("%d",&a);
    printf("Enter value of b=");
    scanf("\n%d",&b);
    printf("Enter value of c=");
    scanf("\n%d",&c);
    int d=a+b+c;
    printf("%d",d);

    return 0;
}
